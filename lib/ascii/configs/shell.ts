import type { AsciiPreviewConfigInput } from "./type";

const shellAsciiConfig = {
  columns: 96,
  contrast: 3,
  invert: true,
} satisfies AsciiPreviewConfigInput;

export default shellAsciiConfig;
