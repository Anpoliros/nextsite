
# 0530 article list

我想调整一下articlelist的列表项。

1. 去除里面的category，只展示tag

2. tag不自动大写

3. tag可点击，hover时tag文字变蓝提示用户

4. desktop模式中，去除左右之间的竖向分界线

5. 移动端模式中，tag改为向左对齐，且不再限制横向长度

说说你的方案，有没有需要我澄清的问题

---

1. 关于点击，我的想法是：

desktop模式，即使去掉竖线也需要左右分区（我不确定），那就让左侧作为进入文章的链接。
modile模式，让上方作为进入文章的链接。

这么想是因为如果只有标题文字能点击的话，点击区域太小了。

2. 补充：我希望任何情况下都限制标签数，且这个数字可以在config/中定义。不过我不确定具体放在那个config文件中比较合适

3. 在mobile模式中，由于去掉了渲染category的容器，tag的容器应该和标题区一样宽了，所以应该不再需要“换行展示”

继续讨论，我的想法不一定合理，还有没有需要澄清的问题

---

有一个小问题，mobile模式下，标题区域和tag区域之间的分割线颜色过于鲜明。我希望它是gray-300 dark:gray-700。而且分割线和标题区域之间的空隙应该和tag文字和分割线之间的空隙一致，现在有点大了。如果你十分确定怎么改就直接改，否则问我问题


# 0530 pinned

探索一下项目，我想把Pinned Articles移动到右侧空间，并让Pinned和Timeline的y轴高度严格对齐。说说你的想法

pinned直接设计一种新的东西，不用articlelist了。




# 0601 vertical

我想让文章页面可选竖排文本。

指定方式：如果articles中的md加了frontmatter VerticalLayout: true，将渲染成竖排。
滚动方式：从右往左读，横向滚动。
英语混排：英语和数字不旋转
图片：单成一段但是混排
代码块、表格：不混排，直接扔到最后

讨论一下可行性和实现方案。





# 0601 background

讨论下网站背景

我想做字符画 + 错位移动的背景

浅色模式：字符云朵。
深色模式：字符星星和流星。

各个对象z序不一样，下划时移动速度也不一样，有一种层次感。

讨论一下，大概要怎么实现，再找几个类似的设计，我觉得肯定有。


例如
```
         ####
      ##     ##
  ###          ##   
#                #
 ################
```
应该有多种变体



---

我们先做个原型机。在开工前先讨论下方案细节。

1. 我希望这个背景组件不带颜色，颜色仍在config中调控。

2. 不同“高度”的景象不一样
    - 浅色模式下，用户的第一个视口中没有任何云，往下划才有，而且到footer的位置是一个全宽度的大云。
    - 深色模式下也有云，但少于浅色模式，最底下也是全宽度大云。星星背景的密度一致。

3. 背景详细说明
高度在顶部1/3时，没有云。
高度在中部1/3时，2~3朵
底部1/3，4~5朵且有大有小，底部还有大云。底部云层会互相遮罩，这样能有明显的错位感。
星星不加错位设计，因为不合常理。

4. 实现
作为一个组件在/components中实现，在app/page.tsx中调用。它本质上是一个小程序，会spawn一些云朵div，赋予它们z-index，然后在滚动时控制它们的行为。目前仅在home做实验，所以放到components/home里就好。

你需要根据我的需求设计一个方案，具体到函数级别。你还可以向我提出想要我澄清的问题，无论是设计上还是实现上。

---

回答：
1. 覆盖首页除了header和footer的部分，不过需要注意它不会遮挡任何其他组件
2. 现在用的nextjs themeprovider吧，你可以看下app/里
3. 可爱风格
4. 大云按你说的实现成小云叠加。footer区域不会再出现该背景
5. 可以

另外，这些字符的颜色应该很浅，需要仔细看才能看出来。这个颜色最好可以配置，整个功能也可以开关。配置放在目前的hero.ts，顺便改名为home.ts。

那么我们来实现吧

---

效果还不错，特别是星星。我有几点

1. 云大一点，大小为现在的2~3倍，而且改成这种形状：
```
         ####
      ##     ##
  ###          ##   
#                #
 ################
```
即云的底部相对平一些。云的边缘内部也可以有增强立体感的修饰。

2. 底部大云现在在不是底部的位置叠成一团，整体偏向左边。我觉得按照网页全宽来动态渲染应该是可行的，对于底部大云，弄三朵云分别宽60% 50% 40%很容易就能占领整个底部。澄清一下，显示范围应该是占领这个组件：
```
body > main
```

3. 我开网页检查器发现，现在的footer竟然中间是分割线，这导致main到footer的分割线之间有一块不归footer管的区域。微调footer让分割线在它的顶部。

4. 云的形状现在似乎是程序生成的，人很难debug，直接写死字符画并随机选择是一个好主意吗

先别开工，说说你对我说的形状的理解，给我画一画。另外后几个issue你有什么问题吗

---

1. #只是示意，还用括号就可以，我找了个图片给你参考，云朵的上方需要至少3个凸起，否则太像包子了

2. 只在首页主体内容底部出现

我们先不用模板，因为我担心如果写死模板会不会容易在不同宽度下出bug

开工吧

---

1. 现在是字符放大了，我是希望整体放大。例子：
```
                .---.
           。--(     )
      .--.(   (     . )
   .-(    ).        )  ）
  (___.__)__)___________)
```
你需要联网搜索具体怎么程序化生成字符画，可以使用其他字符进行生成。而且云朵不要带有表情。

2. 现在底部云成功占据了整个宽度，但其高度仍然高出footer一截。我觉得这个问题是滚动导致的，它本来在正确的位置，但一滚动就向上了。所以底部云应该再低一些，低到在没滚动时占据footer一部分，这样才能刚刚好。

---

这次的形状好很多了，但是我还是希望云能鼓一点，即长宽比降低，顶部凸起稍稍明显一些。另外这次底部云看起来有点太低了，增加一个参数FooterDistance，调整底部云的底部到footer与main交界线的距离，为正时，底部云的底部的高度比交界线更低。这个值的具体名字和量都由你决定


# 0602

看一下components/home/AsciiSceneBackground.tsx，说说现在它的逻辑。我有个想法，把生成字符画的逻辑放到lib/ascii，提供更强大的能力，可以基于svg/jpg/png绘制字符画，并提供精细度控制。甚至将网站中的图片绘制成字符画，以在较差网络环境中提高体验。我们来讨论一下。

---

我们先做一个lib/ascii原型机，不侵入现有系统，实验一下转换图片的效果。

目标：提供一张图片jpg/png/svg/webp，在终端打印出ascii图像。

配置：控制参数放到config/ascii.ts

---

效果还不错。不过有两点

1. config中的参数加个说明，看不懂

2. 现在整体偏密，不容易肉眼看出边界。这是对比度的问题，我发现现在的contrast，0.1时只采用3和5两种字符，5.0时则@非常多。对比度大时应该使用更广泛的字符集，比如可以用上"."":"这样的低密度字符。字符集也应该让用户能够清晰地看到。


# 0605

讨论一下

1. 我发现现在对于背景颜色的处理有一定问题。例如public/images/cloud.jpg这张图片，背景是白色，但转换后不可选地变成了视觉上最深的@。我还是不知道现在lib/ascii的渲染逻辑，颜色会影响结果吗？

2. 对于有透明层的png图片，透明部分的背景应该直接为空，这样就可以用lib/ascii渲染小组件了，说说方案。

3. 换行是另一个棘手的问题。如果在服务端渲染了组件，应该怎么确保在客户端看起来不乱？我有点担心这件事，但也没实践依据


---


JPG jpg jpeg PNG png这种别名也要支持


好，那我们为lib/ascii增加以下功能

1. 支持png，JPG jpg jpeg PNG png这种别名也要支持，webp也需要支持

2. 支持alpha，透明度截断和参与计算两种模式

3. 支持背景色抠图，这个和alpha不要放在一起。除了直接匹配颜色抠图，再定义一个允许的方差范围，即接近白色的也抠掉

4. alpha和background的emptychar分别定义，互不影响，二者可以共存

5. 尾部空格截断也加上



我们讨论一下配置

由于配置越来越多，我觉得可以这样解决配置膨胀问题：

将ascii的配置移动到lib/ascii/configs，
例如
lib/ascii/configs/
  example.ts  # 解释了所有能配置的东西，相当于readme
  type.ts     # 配置数据模型
  default.ts  # 不指定任何参数时的配置
  shell.ts    # 用户自定义配置，通过--config参数使用

用户可以这么使用
npm run ascii:preview -- public/temp/IMG_7324.jpeg --config shell

用户还可以覆盖参数，继续追加的命令行参数将覆盖配置中的参数，
例如
shell.ts中定义invert:true contrast:3
用户执行
npm run ascii:preview -- public/temp/IMG_7324.jpeg --config shell --invert --contrast 5
实际参数
invert:true contrast:5

联网搜索最佳实践，讨论一下


1. 加载规则就用$name

2. 直接迁移，不保留根目录config/ascii.ts

3. --print-config可以加

没问题的话就开工吧

---




对“边界线”的额外优化，识别跳变，强化边界
